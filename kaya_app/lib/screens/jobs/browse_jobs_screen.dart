import 'package:flutter/material.dart';

class BrowseJobsScreen extends StatelessWidget {
  const BrowseJobsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Browse Jobs'),
      ),
      body: const Center(
        child: Text('Browse Jobs Screen - Coming Soon'),
      ),
    );
  }
}
