import 'package:flutter/material.dart';

class BrowseWorkersScreen extends StatelessWidget {
  const BrowseWorkersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Browse Workers'),
      ),
      body: const Center(
        child: Text('Browse Workers Screen - Coming Soon'),
      ),
    );
  }
}
