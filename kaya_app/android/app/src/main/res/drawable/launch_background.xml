<?xml version="1.0" encoding="utf-8"?>
<!--
    Plain white, no logo.

    A logo here was tried and removed. This drawable is a window background
    drawn by Android before Flutter starts, so it cannot animate or lay itself
    out — and a logo that appears for a fraction of a second and vanishes reads
    as a flicker rather than branding. A flat colour matching the app's first
    screen makes the launch look instant instead.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
</layer-list>
